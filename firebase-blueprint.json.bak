{
  "entities": {
    "User": {
      "title": "User",
      "description": "A user profile in the social network",
      "type": "object",
      "properties": {
        "username": { "type": "string", "maxLength": 30 },
        "avatar": { "type": "string", "maxLength": 100 },
        "email": { "type": "string", "maxLength": 150 },
        "followerCount": { "type": "number" },
        "followingCount": { "type": "number" },
        "createdAt": { "type": "number" }
      },
      "required": ["username", "avatar", "createdAt"]
    },
    "Post": {
      "title": "Post",
      "description": "A post containing fitness tips or recipes",
      "type": "object",
      "properties": {
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorAvatar": { "type": "string" },
        "content": { "type": "string", "maxLength": 2000 },
        "tags": { "type": "array", "items": { "type": "string" }, "maxItems": 5 },
        "likesCount": { "type": "number" },
        "commentsCount": { "type": "number" },
        "createdAt": { "type": "number" }
      },
      "required": ["authorId", "authorName", "content", "createdAt"]
    },
    "Comment": {
      "title": "Comment",
      "description": "A comment on a post",
      "type": "object",
      "properties": {
        "postId": { "type": "string" },
        "authorId": { "type": "string" },
        "authorName": { "type": "string" },
        "authorAvatar": { "type": "string" },
        "content": { "type": "string", "maxLength": 500 },
        "createdAt": { "type": "number" }
      },
      "required": ["postId", "authorId", "authorName", "content", "createdAt"]
    },
    "Interaction": {
      "title": "Interaction",
      "description": "A like or save action",
      "type": "object",
      "properties": {
        "userId": { "type": "string" },
        "postId": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["userId", "postId", "createdAt"]
    },
    "Follow": {
      "title": "Follow",
      "description": "A follower relationship",
      "type": "object",
      "properties": {
        "followerId": { "type": "string" },
        "followedId": { "type": "string" },
        "createdAt": { "type": "number" }
      },
      "required": ["followerId", "followedId", "createdAt"]
    }
  },
  "firestore": {
    "/users/{userId}": {
      "schema": { "$ref": "#/entities/User" },
      "description": "User profiles"
    },
    "/posts/{postId}": {
      "schema": { "$ref": "#/entities/Post" },
      "description": "Global feed of posts"
    },
    "/posts/{postId}/comments/{commentId}": {
      "schema": { "$ref": "#/entities/Comment" },
      "description": "Comments on a post"
    },
    "/likes/{likeId}": {
      "schema": { "$ref": "#/entities/Interaction" },
      "description": "Likes by users on posts"
    },
    "/saves/{saveId}": {
      "schema": { "$ref": "#/entities/Interaction" },
      "description": "Saved posts by users"
    },
    "/follows/{followId}": {
      "schema": { "$ref": "#/entities/Follow" },
      "description": "Follower relationships"
    }
  }
}
